// Main entry point for the Markdown Editor application
import '@fortawesome/fontawesome-free/css/all.min.css';
import './styles.css';
import { initializeApplication } from './events/init';

// Initialize the application
initializeApplication();
